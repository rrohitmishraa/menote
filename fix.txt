PHASE 2C — FINAL LINE NUMBER BASELINE ALIGNMENT

The line-number implementation is now functionally correct.

DO NOT change the architecture or behavior.

Everything currently works correctly:

- Line numbers appear correctly
- Numbers update correctly
- Empty lines work correctly
- Numbers do not shift when typing
- Scrolling works
- Text wrapping works
- Enter works
- Editor remains responsive
- Persistence works

There is ONLY one remaining visual issue:

The line numbers sit slightly LOWER than the corresponding text.

Example:

1    Hello
2    World
3    Test

The numbers should be vertically aligned with the text glyphs/baselines.

Current appearance is approximately:

  1    Hello
  2    World
  3    Test

where the numbers are a few pixels too low.

==================================================
FIX ONLY THIS
==================================================

Do not change:

- line-number calculation
- line detection
- scrolling
- wrapping
- NSTextView
- NSScrollView
- persistence
- text storage
- editor font
- editor inset
- gutter width
- spacing
- keyboard behavior

Do not rewrite the implementation.

Only correct the vertical drawing position of the line-number text.

==================================================
CORRECT APPROACH
==================================================

The line number must use the SAME BASELINE as the corresponding editor text.

Do not align using:

- boundingRect.midY
- lineFragmentRect.midY
- vertical centering
- arbitrary fixed offsets

Use the actual baseline position from NSLayoutManager / the editor's text layout.

The line number's text drawing origin should be calculated so that:

line-number baseline == editor text baseline

For example, if the editor text baseline is Y:

    Hello
──────────────  ← text baseline
    1

the baseline of "1" must be exactly on that same Y coordinate.

The number's glyph height is irrelevant; its BASELINE is what must align.

==================================================
IMPORTANT
==================================================

Do NOT solve this by randomly trying:

+1
-1
+2
-2
etc.

If a small visual adjustment is genuinely required because the gutter font differs from the editor font, derive that adjustment from the font metrics rather than guessing.

Prefer using the same font family/weight as the editor with an appropriately smaller size so the baseline metrics remain predictable.

==================================================
TEST
==================================================

Use:

Hello
World
Third line


Verify visually that:

1    Hello
2    World
3    Third line

has matching text baselines.

Then test:

- empty lines
- newly created lines
- long wrapped lines
- vertical scrolling
- typing
- Enter

Nothing else should change.

Build and launch.

Do NOT implement any other feature.

This is a final visual alignment correction only.