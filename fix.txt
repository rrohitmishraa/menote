IMPORTANT UI CORRECTION — READ THIS CAREFULLY

The current UI is WRONG.

I do NOT want:
- translucent backgrounds
- blue/colored/vibrancy backgrounds
- gradients
- glass effects
- blurred backgrounds
- rounded cards inside the window
- visible borders around sections
- colored side panels
- a separate colored footer
- a separate colored toolbar background

I want the ENTIRE Menote popover to be ONE SOLID WHITE BLOCK.

Use the attached screenshot as the reference for the CURRENT problem.

DESIRED STRUCTURE:

┌──────────────────────────────────────────────┐
│  Toolbar                                     │
│  file  share  B  /  ● ● ● ● ●        search │
├──────────────────────────────────────────────┤
│                                              │
│  1   Editor content                          │
│  2                                           │
│  3                                           │
│                                              │
│                                              │
│                                              │
├──────────────────────────────────────────────┤
│  Ready                              0 words  │
└──────────────────────────────────────────────┘

The entire rectangle above must be SOLID WHITE.

The ONLY visual separation between the three areas should be thin horizontal separator lines:

1. Toolbar
──────────────────────────────────────────────

2. Editor
──────────────────────────────────────────────

3. Status/footer

There should NOT be vertical separators.

There should NOT be a colored background behind the line numbers.

There should NOT be a different background behind the footer.

There should NOT be a different background behind the toolbar.

Everything is one white surface.

IMPORTANT DETAIL:

The line-number gutter should also be white.

So:

Toolbar       → white
Line numbers  → white
Editor        → white
Status bar    → white

No transparency anywhere.

NO NSVisualEffectView / vibrancy / material / blur should be visible anywhere in the Menote popover.

If NSVisualEffectView is currently being used as the main background, remove it or disable its visual effect and replace the background with an opaque white NSView.

The popover itself should also have an opaque white appearance.

Do NOT rely on the desktop/window behind Menote for any visual effect.

The final result should remain completely white regardless of what is behind the popover.

BORDERS:

Do not use a surrounding border around the UI.

Do not put a border around the editor.

Do not put a border around the line-number gutter.

Do not put borders around toolbar controls.

The only separators should be the two thin horizontal lines between:
- toolbar / editor
- editor / status bar

These separators should be subtle and neutral gray.

LAYOUT:

Keep the existing functionality and dimensions.

Do NOT redesign the toolbar.

Keep:
- file/open
- share/export
- bold
- italic
- text colors
- find
- line numbers
- editor
- status
- About
- word count

Only change the visual background/structure.

LINE NUMBERS:

Line numbers must sit directly on the same white surface as the editor.

They should remain muted gray.

They must NOT be inside a colored gutter.

They must NOT have a vertical separator.

They should visually appear as part of the editor:

    1   Hello world
    2   This is another line
    3   Another line

not:

    [gray gutter] | editor

The gutter is simply white space.

POPOVER CORNERS:

Keep the existing rounded outer popover shape if it is required by the macOS popover/window.

But the INSIDE must still be one continuous white surface.

Do not create an additional rounded white card inside the popover.

The white surface should fill the entire popover content area.

COLORS:

Background:
#FFFFFF

Separator:
a very subtle neutral gray, e.g. #E5E5E5

Text:
existing text color

Line numbers:
existing muted gray

Do not introduce blue, purple, cyan, gradient, translucency, or vibrancy into the background.

IMPORTANT:

This is NOT asking you to make only the editor white.

I want the WHOLE Menote interface to be white.

The toolbar, editor, line-number area, and footer/status area must all be the SAME opaque white background.

Use simple horizontal rules to establish hierarchy.

Before finishing, run the app and visually verify that placing Menote over:
- a blue desktop
- a dark desktop
- another application
- an image

produces exactly the same solid-white Menote surface.

The background behind Menote must have ZERO influence on the appearance of the Menote UI.

Do not change functionality while making this change.