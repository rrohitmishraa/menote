Increase Menote's undo history to 500 steps.

The current undo implementation is working correctly. DO NOT rewrite it.

Use the existing native undo system and simply increase its history capacity to 500 undoable operations.

500 is the minimum target.

If the existing implementation naturally supports unlimited undo without storing full document snapshots, do not artificially impose a 500-step limit.

IMPORTANT:
Performance matters more than the exact number.

Do NOT store a complete copy of the entire document for every undo step if the current architecture can record incremental text mutations instead.

The editor must remain responsive while:
- typing
- deleting
- undoing
- redoing
- working with large documents

Test with at least 500 consecutive edits.

Verify:
⌘Z can walk backward through the history.
⇧⌘Z can walk forward again.

Do not modify:
- list behavior
- deletion behavior
- paste behavior
- Enter/Shift+Enter
- formatting
- UI

Only increase/optimize the existing undo history.